import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    position: "absolute",
    width: "100%",
    justifyContent: "center",
    alignItems: "flex-end",
    paddingBottom: 12,
    height: 84,
    top: 0,
    backgroundColor: "#4863f7",
  },
  title: { color: "#fff", fontSize: 16, marginLeft: 7 },
});
